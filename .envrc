export PROJECT_DIR=${PWD}
export SECRETS_DIR=${PROJECT_DIR}/secrets
export WORK_DIR=${PROJECT_DIR}/work

PATH=${PROJECT_DIR}/bin:${PATH}

watch_file "${SECRETS_DIR}/kubeconfig"
if [ -f "${SECRETS_DIR}/kubeconfig" ] ; then
  export KUBECONFIG="${SECRETS_DIR}/kubeconfig"  
fi

export PARAMS_YAML=${SECRETS_DIR}/params.yml
export TF_VAR_vcenter_password=${GOVC_PASSWORD}
export TF_VAR_domain=shortrib.net
export TF_VAR_email=cdantonio@vmware.com
export TF_VAR_project_root=${PROJECT_DIR}
export TF_VAR_environment=lab

layout virtualenvwrapper tanzu
unset PS1
